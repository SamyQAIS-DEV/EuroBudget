export enum HttpResponseCodeEnum {
    HTTP_UNPROCESSABLE_ENTITY = 422,
    HTTP_NOT_FOUND = 404,
    HTTP_FORBIDDEN = 403,
    HTTP_OK = 200,
    HTTP_NO_CONTENT = 204,
}

export enum HttpRequestMethodEnum {
    GET = 'GET',
    POST = 'POST',
    PUT = 'PUT',
    PATCH = 'PATCH',
    DELETE = 'DELETE',
}