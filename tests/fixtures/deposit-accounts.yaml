include:
    - ../../fixtures/templates.yaml

App\Entity\DepositAccount:
    depositAccount{1..5} (extends depositAccount):

    depositAccountWithoutOperation:
        creator: '@admin_user'
        users: [ '@admin_user' ]
        title: Title no operation
        amount: 0
        color: '#287bff'