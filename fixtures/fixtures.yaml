include:
    - templates.yaml

App\Entity\User:
    user{1..5} (extends user):
    admin (extends user):
        email: samyqais@domain.fr
    github_user  (extends user):
        githubId: 12345
        email: github_user@domain.fr
