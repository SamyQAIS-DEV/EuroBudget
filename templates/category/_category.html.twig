<div class="category grid fit">
    <div class="category__title">
        <span>{{ category.name }}</span>
    </div>
    <div class="category__actions actions">
        <button>
            <a href="{{ path('categories_edit', {'id': category.id}) }}" class="btn-primary" title="Éditer la catégorie">
                {{ icon('edit') }}
            </a>
        </button>
        {{ include('category/_delete_form.html.twig') }}
    </div>
</div>