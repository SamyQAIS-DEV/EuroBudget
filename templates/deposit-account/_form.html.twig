{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.amount) }}
    {{ form_row(form.color) }}

    <button type="submit" class="btn btn-primary">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}
